# --- Shared infrastructure ---
DATABASE_URL=postgres://pulsegrid:pulsegrid@localhost:5432/pulsegrid?sslmode=disable
REDIS_URL=redis://localhost:6379
KAFKA_BROKERS=localhost:19092

# --- Control-plane API (apps/api) ---
PORT=4000
WEB_ORIGIN=http://localhost:3000
NOTIFICATION_URL=http://localhost:7104

# --- Dashboard (apps/web) ---
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=ws://localhost:4000/ws

# --- Go services (each also accepts SERVICE/PORT/DOWNSTREAM_URL) ---
ENVIRONMENT=demo
REGION=local
BASE_RPS=3
# SEED=42            # deterministic traffic randomness for tests
